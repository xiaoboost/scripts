import { createStyle } from '@scripts/utils';

export default createStyle({
  heataiCheckbox: {
    paddingLeft: 10,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
