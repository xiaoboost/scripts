{
  "name": "scripts",
  "version": "1.0.0",
  "description": "个人用的脚本仓库",
  "author": "xiaoboost",
  "license": "GPLv3.0",
  "scripts": {
    "build:dep": "pnpm run build --filter @scripts/builder...",
    "build": "pnpm run build --filter @scripts/zhihu --filter @scripts/e-hentai",
    "lint": "pnpm run -r lint --parallel --filter .",
    "lint:fix": "pnpm run -r lint:fix --parallel --filter ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/xiaoboost/scripts.git"
  },
  "engines": {
    "pnpm": ">= 6.0.0",
    "node": ">= 12"
  },
  "devDependencies": {
    "@scripts/tsconfig": "workspace:*",
    "@typescript-eslint/eslint-plugin": "^5.8.1",
    "@typescript-eslint/parser": "^5.9.0",
    "eslint": "^8.6.0",
    "eslint-plugin-react": "^7.28.0",
    "typescript": "^4.5.4"
  }
}
